{
  "name": "projeton1",
  "version": "1.0.0",
  "description": "This project is made for the discipline Programing Mobile",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && cd tours && npm install && cd ../server && npm install",
     "start": "concurrently \"npm run server\" \"npm run client\"",
    "db": "cd server && npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "client": "cd tours && npx expo start",
    "server": "cd server && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
